# Runtime dependencies. Install with:  pip install -r requirements.txt
#
# Lower bounds are the oldest versions the test suite is known to pass on;
# there is no upper pin, and analyse_raman.py works on both NumPy 1.x
# (np.trapz) and NumPy 2.x (np.trapezoid).

numpy>=1.24
scipy>=1.10

# plot_spectrum.py -- the annotated figure in the README
matplotlib>=3.7

# Raman_analysis.py -- the alternative AsLS implementation kept for comparison
plotly>=5.0

# tests/ -- the ground-truth eval harness
pytest>=7.0
