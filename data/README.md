Put the sample spectra here as `.txt` files, two columns (wavenumber,
intensity). Filenames containing `chex`/`ch-ex`/`treated`/`coated` are
auto-labelled Ch/Ex; those containing `control`/`ctrl`/`reference` as Control.
