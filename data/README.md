Put the sample spectra here as `.txt` files, two columns (wavenumber,
intensity). Filenames containing `chex`/`ch-ex`/`coated` are auto-labelled
as the treated sample; those containing `ctrl`/`reference`/`control` as the control.
